[package]
name = "miros"
version = "0.1.0"
edition = "2021"
description = "A modern ELF interpreter (dynamic linker/loader) written in Rust"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
